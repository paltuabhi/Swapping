#include<bits/stdc++.h>
using namespace std;
int swap(int *a,int *b)
{
    *a=*a+*b;
    *b=*a-*b;
    *a=*a-*b;
    return *a;
    return *b;
}
int main()
{
    int n,m;
    cin>>n>>m;
    swap(&n,&m);
    cout<<n<<endl<<m;
    return 0;
}
